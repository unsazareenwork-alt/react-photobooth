# Korean Photobooth

## Setup
1. npm install
2. npm run dev
3. Open the localhost URL it prints, allow camera access when prompted.

Camera access requires localhost or HTTPS — it will not work over plain http:// on a network IP.
